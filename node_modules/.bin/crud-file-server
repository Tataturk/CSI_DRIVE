#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../crud-file-server/bin/crud-file-server" "$@"
  ret=$?
else 
  node  "$basedir/../crud-file-server/bin/crud-file-server" "$@"
  ret=$?
fi
exit $ret
